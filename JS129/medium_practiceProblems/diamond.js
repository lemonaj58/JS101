const abc = 'abcdefghijklmnopqrstuvwxyz';

class Diamond {
  constructor() {
    this.abc = abc;
  }

  static makeDiamond(letter) {
    let spaces = abc.indexOf(letter);

  }

  static firstPart(spaces) {
    for (let index = spaces; index >= 0; index -= 1);
  }
}
